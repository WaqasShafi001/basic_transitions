import 'package:flutter/material.dart';

class SlideCustomTransition extends StatelessWidget {
  const SlideCustomTransition({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
      ),
    );
  }
}
